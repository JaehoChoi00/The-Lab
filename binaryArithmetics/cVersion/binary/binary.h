#ifndef BINARY_H
#define BINARY_H

char* shiftLeft(char binary[]);
// char* shiftLeftNum(char binary[], int number);
// char* shiftRight(char binary[]);
// char* shiftRightNum(char binary[], int number);

char* flip(char binary[]);

int binaryToVariable(char binary[]);

// char* convertToBinary(int variable);

// char* convertToBinaryFixed(int variable, int targetLength);

// char* convertToHex(char binary[]);

// char* hexToBinary(char hex[]);

// int* convertToByteUTF8(char textToConvert[]);

#endif